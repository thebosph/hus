import { OpCall } from '../nodeTypes';
import { Value } from '../values';
declare type GroqOperatorFn = (left: Value, right: Value) => Value | PromiseLike<Value>;
export declare const operators: {
    [key in OpCall]: GroqOperatorFn;
};
export {};
