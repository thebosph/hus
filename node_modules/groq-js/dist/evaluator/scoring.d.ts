import { Executor } from './types';
import { ExprNode } from '../nodeTypes';
import { Scope } from './scope';
export declare function evaluateScore(node: ExprNode, scope: Scope, execute: Executor): Promise<number>;
