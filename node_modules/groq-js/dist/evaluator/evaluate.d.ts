import * as NodeTypes from '../nodeTypes';
import { Scope } from './scope';
import { EvaluateOptions, Executor } from './types';
import { Value } from '../values';
export declare function evaluate(node: NodeTypes.ExprNode, scope: Scope, execute?: Executor): Value | PromiseLike<Value>;
/**
 * Evaluates a query.
 */
export declare function evaluateQuery(tree: NodeTypes.ExprNode, options?: EvaluateOptions): Value | PromiseLike<Value>;
