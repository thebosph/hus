import { Value } from './types';
export declare class DateTime {
    date: Date;
    constructor(date: Date);
    static parseToValue(str: string): Value;
    equals(other: DateTime): boolean;
    add(secs: number): DateTime;
    difference(other: DateTime): number;
    compareTo(other: DateTime): number;
    toString(): string;
    toJSON(): string;
}
export declare function parseRFC3339(str: string): Date | null;
export declare function formatRFC3339(d: Date): string;
